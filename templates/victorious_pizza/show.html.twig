{% extends 'base.html.twig' %}

{% block title %}VictoriousPizza{% endblock %}

{% block body %}
    <h1>VictoriousPizza</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ victorious_pizza.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ victorious_pizza.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('victorious_pizza_index') }}">back to list</a>

    <a href="{{ path('victorious_pizza_edit', {'id': victorious_pizza.id}) }}">edit</a>

    {{ include('victorious_pizza/_delete_form.html.twig') }}
{% endblock %}
